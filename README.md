The basic SQL functions were implemented in C++, in the form of a dynamic library for speed, which is then linked with JavaFX for
interaction between the C++ backend and the Java GUI. The graphical representation for user interaction is made in JavaFX. 
